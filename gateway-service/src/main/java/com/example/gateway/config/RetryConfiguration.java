package com.example.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for retry mechanism in Spring Cloud Gateway
 * Retry configuration will be handled through application.yml
 */
@Configuration
public class RetryConfiguration {
    // Retry configuration is handled through YAML configuration
    // This class is kept for future programmatic configuration needs
}