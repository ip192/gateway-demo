package com.example.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for timeout handling in Spring Cloud Gateway
 * Timeout configuration will be handled through application.yml
 */
@Configuration
public class TimeoutConfiguration {
    // Timeout configuration is handled through YAML configuration
    // This class is kept for future programmatic configuration needs
}